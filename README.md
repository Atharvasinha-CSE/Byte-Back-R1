# Byte-Back-R1
A lightweight, Gradio-based streaming interface for DeepSeek models running on cloud GPUs via Hugging Face Transformers. It implements real-time regex sanitization to hide internal reasoning tokens and uses asynchronous text generation for seamless chat interactions.
